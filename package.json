{
  "name": "henhacks-2026",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "concurrently -n FRONTEND,BACKEND -c blue,green \"npm run dev -w frontend\" \"npm run start:dev -w backend\""
  },
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "devDependencies": {
    "concurrently": "^9.2.1"
  }
}
